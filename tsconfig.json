{
  "extends": "@mate-academy/students-ts-config",
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.js", // Opcional, se você tiver arquivos JS
    "src/**/*.jsx" // Opcional, se você tiver arquivos JSX
  ],
  "compilerOptions": {
    "sourceMap": false,
    "types": ["node", "cypress"]
  }
}
